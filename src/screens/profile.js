import React from "react";
import { StyleSheet, View } from "react-native";

const Profile = () => {
  return <View styles={styles.container}>Profile</View>;
};

const styles = StyleSheet.create({
  container: {},
});

export default Profile;
